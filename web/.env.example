# AutoStepper Web Environment Configuration

# In Tauri desktop app, YouTube downloads are handled by the Rust backend.
# No environment variables are required for normal operation.

# For standalone web development (advanced, requires custom backend):
# VITE_DOWNLOAD_API_URL=http://localhost:5000/download
