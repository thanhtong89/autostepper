# AutoStepper Web Environment Configuration
# Copy this file to .env and configure:
#   cp .env.example .env

# YouTube Download API URL
# -------------------------------------------------

# OPTION 1: Local Development Server
# Run: cd infrastructure/local && python server.py
VITE_LAMBDA_URL=http://localhost:5000/download

# OPTION 2: AWS Lambda (Production)
# Deploy first: cd infrastructure/terraform && terraform apply
# Get URL: terraform output lambda_function_url
# VITE_LAMBDA_URL=https://xxxxx.lambda-url.us-east-1.on.aws/
